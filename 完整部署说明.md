# xiaoruru Docker 完整部署说明

本文描述在全新 Ubuntu 24.04 服务器上部署当前项目，并把另一个 Compose 项目中的 CPA Manager Plus、CLI Proxy API 接入同一反向代理网络。

已有服务器只接入博客时，使用 [博客部署说明](./博客部署说明.md) 的增量部署流程，不必执行完整初始化。

## 1. 架构与端口

```text
Internet
├── 80/TCP   -> Nginx UI 内置 Nginx（ACME、HTTP 跳转）
├── 443/TCP  -> Nginx UI 内置 Nginx
│   ├── nginx-ui.xiaoruru.beer      -> 127.0.0.1:9000
│   ├── 3x-ui.xiaoruru.beer         -> xui:2053 / xui:2096
│   ├── cpa-mp.xiaoruru.beer        -> cpa-manager-plus:18317
│   ├── cli-proxy-api.xiaoruru.beer -> cli-proxy-api:8317
│   └── xiaoruru.beer              -> rurublog:8080
├── 8443/TCP -> 3x-ui/Xray（VLESS + Reality + Vision）
├── 8443/UDP -> 3x-ui/Xray（Hysteria2 + TLS）
└── 9443/TCP -> GOST（带账号密码的 HTTPS 正向代理）

Docker 外部网络 xiaoruru-gateway
├── xiaoruru-stack：nginx-ui、xui、gost、rurublog
└── CPAMP 项目：cpa-manager-plus、cli-proxy-api
```

宿主机不发布 `9000`、`2053`、`2096`、`18317`、`8317` 和 `8080`。Nginx UI 通过共享 Docker 网络和服务名访问两个 CPAMP 容器和博客。

这里必须显式配置同一个外部网络。若两个 Compose 文件都省略 `networks`，Docker 会分别创建类似 `xiaoruru_default` 和 `cpamp_default` 的网络，两个项目仍然无法按容器名通信。

## 2. 域名解析

部署前，将以下 A 记录全部指向 `198.11.173.173`：

| 域名 | 用途 |
| --- | --- |
| `nginx-ui.xiaoruru.beer` | Nginx UI 管理界面 |
| `3x-ui.xiaoruru.beer` | 3x-ui 面板、订阅、VPN 节点地址 |
| `cpa-mp.xiaoruru.beer` | CPA Manager Plus 管理界面 |
| `cli-proxy-api.xiaoruru.beer` | CLI Proxy API |
| `www.xiaoruru.beer` | GOST HTTPS 正向代理证书名 |
| `xiaoruru.beer` | 博客首页与博客后台 |

以上六个域名共用 `xiaoruru-services` SAN 证书，由 Certbot 签发和续期。不是通配符证书，不需要额外的博客证书。Nginx UI 负责站点配置，不要再用另一套证书管理流程接管同一张证书。

在本地检查解析：

```bash
for name in \
  nginx-ui.xiaoruru.beer \
  3x-ui.xiaoruru.beer \
  cpa-mp.xiaoruru.beer \
  cli-proxy-api.xiaoruru.beer \
  www.xiaoruru.beer \
  xiaoruru.beer; do
  printf '%-35s ' "$name"
  dig +short A "$name"
done
```

## 3. 安全组和防火墙

云安全组允许：

| 端口 | 协议 | 用途 |
| --- | --- | --- |
| 22 | TCP | SSH |
| 80 | TCP | HTTP 与 ACME 验证 |
| 443 | TCP | 所有 HTTPS 网站 |
| 8443 | TCP | VLESS Reality |
| 8443 | UDP | Hysteria2 |
| 9443 | TCP | GOST HTTPS 正向代理 |

不要开放 `9000`、`2053`、`2096`、`18317`、`8317` 和 `8080`。

## 4. 安装 Docker

在服务器执行：

```bash
apt-get update
apt-get install -y ca-certificates curl gnupg
install -m 0755 -d /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg \
  -o /etc/apt/keyrings/docker.asc
chmod a+r /etc/apt/keyrings/docker.asc

. /etc/os-release
cat >/etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://download.docker.com/linux/ubuntu
Suites: ${UBUNTU_CODENAME:-$VERSION_CODENAME}
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

apt-get update
apt-get install -y docker-ce docker-ce-cli containerd.io \
  docker-buildx-plugin docker-compose-plugin sqlite3 dnsutils python3 openssl rsync
systemctl enable --now docker
docker version
docker compose version
```

## 5. 上传项目

在本地电脑使用 JDK 21 和 Maven 构建博客 JAR，再上传项目（不要求本机运行 Docker）：

```bash
cd /Users/jun/IdeaProjects/docker-xiaoruru
mvn -f xiaoruru-blog/pom.xml -B -ntp verify \
  '-DargLine=-Dspring.config.location=classpath:/application.yml'
rsync -avz \
  --exclude '.git/' --exclude '.idea/' --exclude '.DS_Store' \
  --exclude '.env' --exclude '*.tar' --exclude '*.tar.gz' \
  --exclude '/data/' \
  --exclude '/config/rurublog/application.yml' \
  --exclude '/config/rurublog/application.yaml' \
  --exclude '/xiaoruru-blog/data/' \
  --exclude '/xiaoruru-blog/config/application.yml' \
  --exclude '/xiaoruru-blog/config/application.yaml' \
  --include '/xiaoruru-blog/target/' \
  --include '/xiaoruru-blog/target/rurublog.jar' \
  --exclude '/xiaoruru-blog/target/*' \
  -e 'ssh -i /Users/jun/Downloads/初始化搭建.pem' \
  /Users/jun/IdeaProjects/docker-xiaoruru/ \
  root@198.11.173.173:/srv/xiaoruru-stack/
```

上传不覆盖服务器 `.env`、私密博客配置和运行数据。源路径末尾保留 `/`，不会额外嵌套项目目录。服务器进入目录并构建运行镜像；此步骤只打包 JAR，不执行 Maven：

```bash
cd /srv/xiaoruru-stack
ls -la
chmod +x scripts/*.sh
docker build --target prebuilt -t xiaoruru/rurublog:local ./xiaoruru-blog
```

`.env` 是隐藏文件，所以普通 `ls` 不显示，使用 `ls -la` 查看。

## 6. 配置 `.env`

若上传目录中没有 `.env`：

```bash
cd /srv/xiaoruru-stack
cp .env.example .env
chmod 600 .env
nano .env
```

完整示例：

```dotenv
COMPOSE_PROJECT_NAME=xiaoruru
TZ=Asia/Shanghai

XUI_DOMAIN=3x-ui.xiaoruru.beer
NGINX_UI_DOMAIN=nginx-ui.xiaoruru.beer
CPAMP_DOMAIN=cpa-mp.xiaoruru.beer
CPA_API_DOMAIN=cli-proxy-api.xiaoruru.beer
GOST_DOMAIN=www.xiaoruru.beer
BLOG_DOMAIN=xiaoruru.beer
CERT_NAME=xiaoruru-services
LE_EMAIL=你的邮箱
LE_STAGING=0

XUI_IMAGE=ghcr.io/mhsanaei/3x-ui:v3.7.0
NGINX_UI_IMAGE=uozi/nginx-ui:v2.5.10
GOST_IMAGE=gogost/gost:3.2.6
CERTBOT_IMAGE=certbot/certbot:v5.7.0
PANEL_INIT_IMAGE=python:3.13.7-alpine3.22

HTTP_PORT=80
HTTPS_PORT=443
XRAY_PORT=8443
GOST_PORT=9443

XUI_PANEL_PORT=2053
XUI_SUB_PORT=2096
XUI_WEB_BASE_PATH=/
XUI_MEMORY_LIMIT_MB=400

PRIMARY_CLIENT_EMAIL=jun-main
REALITY_MIN_CLIENT_VER=1.0.0
REALITY_TARGET_CANDIDATES=

GATEWAY_NETWORK=xiaoruru-gateway
DOCKER_SUBNET=172.30.60.0/24

BLOG_IMAGE=xiaoruru/rurublog:local
BLOG_BUILD_TARGET=prebuilt
BLOG_UID=10001
BLOG_GID=10001
BLOG_MEMORY_LIMIT=768m
BLOG_CPUS=1.0

XUI_ADMIN_USERNAME=替换为管理员用户名
XUI_ADMIN_PASSWORD=替换为长随机密码
GOST_USERNAME=替换为代理用户名
GOST_PASSWORD=替换为长随机密码
```

`LE_EMAIL` 可留空；填写有效邮箱后，Certbot 和 Nginx UI 都会使用它。`GOST_USERNAME` 与 `GOST_PASSWORD` 只使用英文字母、数字、下划线和短横线。

博客最大堆默认 320MiB、容器内存上限 768MiB；自定义 JVM 参数使用 `.env.example` 中单引号包裹的 `BLOG_JAVA_OPTIONS`。博客私密配置由初始化脚本生成在 `config/rurublog/application.yml`，不放在根 `.env`，避免密码/BCrypt 中的 `$` 被 Shell 展开。

## 7. 一键部署

在服务器执行：

```bash
cd /srv/xiaoruru-stack
./scripts/validate.sh
./scripts/bootstrap.sh
```

脚本会自动完成：

1. 创建 `xiaoruru-gateway` 外部 Docker 网络；
2. 启动 3x-ui 和 Nginx UI；
3. 自动创建 Nginx UI 初始管理员；
4. 启用临时 HTTP 站点；
5. 为六个域名签发一张 Let's Encrypt SAN 证书，已有证书缺少主域名时自动扩展；
6. 生成并启用五个 HTTPS 站点；
7. 配置 3x-ui 面板和三种订阅地址；
8. 创建 VLESS Reality `8443/TCP` 入站；
9. 创建 Hysteria2 `8443/UDP` 入站；
10. 创建主客户端并生成订阅和单节点链接；
11. 启动 GOST `9443/TCP`；
12. 生成博客独立凭据、初始化 `data/rurublog/`，启动博客并等待健康检查通过。

部署完成后：

```text
Nginx UI: https://nginx-ui.xiaoruru.beer/
3x-ui:     https://3x-ui.xiaoruru.beer/
CPAMP:     https://cpa-mp.xiaoruru.beer/
CPA API:   https://cli-proxy-api.xiaoruru.beer/
博客:      https://xiaoruru.beer/
博客后台:  https://xiaoruru.beer/admin/login
```

Nginx UI 和 3x-ui 初始账号密码均为 `.env` 中的：

```bash
grep -E '^XUI_ADMIN_(USERNAME|PASSWORD)=' /srv/xiaoruru-stack/.env
```

## 8. 接入 CPAMP Compose 项目

主项目首次执行 `bootstrap.sh` 后，共享网络 `xiaoruru-gateway` 已存在。

项目提供了 [CPAMP Compose 覆盖文件](./examples/cpamp-compose.override.yaml)。将它复制到 CPAMP 项目目录，并与 CPAMP 原有 Compose 文件一起启动。下面假设原文件名是 `compose.yaml`：

```bash
cd /你的/CPAMP/项目目录
cp /srv/xiaoruru-stack/examples/cpamp-compose.override.yaml \
  ./compose.gateway.yaml
docker compose -f compose.yaml -f compose.gateway.yaml up -d --force-recreate
```

若原文件名不是 `compose.yaml`，将命令中的第一个文件名替换为实际文件名。

覆盖文件完成两件事：

- 将 `cpa-manager-plus` 和 `cli-proxy-api` 加入 `xiaoruru-gateway`；
- 清除宿主机的 `18317:18317`、`8317:8317` 映射，只在 Docker 网络内暴露端口。

检查网络成员：

```bash
docker network inspect xiaoruru-gateway \
  --format '{{range $id, $c := .Containers}}{{println $c.Name}}{{end}}'
```

应看到：

```text
xiaoruru-nginx-ui
xiaoruru-xui
xiaoruru-gost
xiaoruru-rurublog
cpamp-cpa-manager-plus-1
cpamp-cli-proxy-api-1
```

CPA 配置中需要允许 Docker 网络内的远程管理请求：

```yaml
remote-management:
  secret-key: '你的 CPA Management Key'
  allow-remote: true
```

CPAMP 首次设置填写：

```text
CPA URL:            http://cli-proxy-api:8317
CPA Management Key: CPA 配置中的 remote-management.secret-key
```

不要把 CPA URL 填成公网子域名，否则会形成不必要的代理回环。

## 9. 使用 Nginx UI

访问：

```text
https://nginx-ui.xiaoruru.beer/
```

登录后可在界面右上角切换中文。当前项目生成的站点会显示在“站点管理”中：

```text
00-default.conf
10-nginx-ui.conf
20-3x-ui.conf
30-cpa-manager-plus.conf
40-cli-proxy-api.conf
50-rurublog.conf
```

在界面中编辑站点并保存时，Nginx UI 会执行配置检查和热重载。持久化位置：

```text
data/nginx/       Nginx 配置
data/nginx-ui/    Nginx UI 数据库与应用设置
data/letsencrypt/ 共享证书
data/logs/nginx/  Nginx 日志
```

以后新增反向代理时，将目标容器加入 `xiaoruru-gateway`，然后在 Nginx UI 中用 `容器服务名:容器端口` 作为上游。例如：

```text
http://my-service:8080
```

本项目没有把 `/var/run/docker.sock` 挂入 Nginx UI。管理内置 Nginx 不需要 Docker Socket，同时可避免把宿主机 Docker 管理权限交给 Web 面板。Compose 已设置 `NGINX_UI_IGNORE_DOCKER_SOCKET=true`，Docker Socket 自检不会报错。

## 10. VPN 入站和订阅

自动创建：

```text
VLESS-Reality-8443
协议：VLESS
传输：RAW/TCP
安全：Reality
端口：8443/TCP
分享地址：3x-ui.xiaoruru.beer
Flow：xtls-rprx-vision

Hysteria2-8443
协议：Hysteria2
端口：8443/UDP
SNI：3x-ui.xiaoruru.beer
ALPN：h3
```

证书路径：

```text
/etc/letsencrypt/live/xiaoruru-services/fullchain.pem
/etc/letsencrypt/live/xiaoruru-services/privkey.pem
```

订阅和节点链接位于：

```bash
chmod 600 /srv/xiaoruru-stack/data/bootstrap-output/access.json
cat /srv/xiaoruru-stack/data/bootstrap-output/access.json
```

其中：

- `subscriptions.base`：Shadowrocket、v2rayN 等基础订阅；
- `subscriptions.json`：Xray JSON 订阅；
- `subscriptions.clash`：Clash Mi/Mihomo 订阅；
- `links`：VLESS Reality 和 Hysteria2 单节点链接。

## 11. GOST HTTPS 正向代理

```text
类型：HTTPS Proxy
服务器：www.xiaoruru.beer
端口：9443
用户名：.env 中的 GOST_USERNAME
密码：.env 中的 GOST_PASSWORD
```

外部测试：

```bash
curl \
  --proxy https://www.xiaoruru.beer:9443 \
  --proxy-user '用户名:密码' \
  https://api.ipify.org
```

返回值应为服务器公网出口 IP。

## 12. 验收

服务器侧：

```bash
cd /srv/xiaoruru-stack
docker compose ps
docker stats --no-stream
ss -lntup | grep -E ':(80|443|8443|9443)\b'

curl -I https://nginx-ui.xiaoruru.beer/
curl -I https://3x-ui.xiaoruru.beer/
curl -I https://cpa-mp.xiaoruru.beer/management.html
curl -i https://cli-proxy-api.xiaoruru.beer/healthz
curl -I https://xiaoruru.beer/
docker compose exec -T rurublog /app/healthcheck.sh
```

确认内部端口未发布：

```bash
docker ps --format 'table {{.Names}}\t{{.Ports}}'
```

确认 3x-ui 数据库对象：

```bash
sqlite3 data/x-ui/x-ui.db \
  "SELECT id, remark, protocol, port, enable FROM inbounds ORDER BY id;"

sqlite3 data/x-ui/x-ui.db \
  "SELECT email, length(uuid), length(auth), flow, enable FROM clients;"
```

## 13. 证书自动续期

手动执行一次检查：

```bash
cd /srv/xiaoruru-stack
./scripts/renew-certificate.sh
```

启用每日定时检查：

```bash
cd /srv/xiaoruru-stack
install -m 0644 config/systemd/xiaoruru-cert-renew.service /etc/systemd/system/
install -m 0644 config/systemd/xiaoruru-cert-renew.timer /etc/systemd/system/
systemctl daemon-reload
systemctl enable --now xiaoruru-cert-renew.timer
systemctl list-timers xiaoruru-cert-renew.timer --all
```

证书更新后，脚本会热重载 Nginx，并重启 GOST 和 3x-ui 以载入新证书。

博客与其他网站会随 Nginx 热重载使用新证书，不需要重启博客容器。Reality 本身不使用这张证书；3x-ui 中的 Hysteria2 使用它。新增主域名的证书扩展由 `scripts/ensure-certificate.sh` 处理；日常 `renew-certificate.sh` 续期的是扩展后的同一张证书。

## 14. 日常命令

```bash
cd /srv/xiaoruru-stack

docker compose ps
docker compose logs -f --tail=100 nginx-ui
docker compose logs -f --tail=100 xui
docker compose logs -f --tail=100 gost
docker compose logs -f --tail=100 rurublog

docker compose restart nginx-ui
docker compose --profile ops run --rm panel-init
```

手工检查 Nginx 配置：

```bash
docker compose exec nginx-ui nginx -t
docker compose exec nginx-ui nginx -s reload
```

## 15. 常见问题

### Certbot 返回 403

逐个确认六个域名都解析到本机，并且 80/TCP 能从公网访问；如果存在 AAAA 记录，IPv6 也必须可用：

```bash
dig +short A nginx-ui.xiaoruru.beer
dig +short A 3x-ui.xiaoruru.beer
dig +short A cpa-mp.xiaoruru.beer
dig +short A cli-proxy-api.xiaoruru.beer
dig +short A www.xiaoruru.beer
dig +short A xiaoruru.beer
docker compose logs --tail=200 nginx-ui
```

### CPAMP 子域名返回 502

```bash
docker network inspect xiaoruru-gateway \
  --format '{{range $id, $c := .Containers}}{{println $c.Name}}{{end}}'

docker exec xiaoruru-nginx-ui getent hosts cpa-manager-plus
docker exec xiaoruru-nginx-ui getent hosts cli-proxy-api
```

两个服务名都必须能解析。若不能，重新用 CPAMP 主 Compose 文件和 `compose.gateway.yaml` 一起创建容器。

### CPAMP 能打开但不能连接 CPA

确认 CPAMP 中的 CPA URL 是：

```text
http://cli-proxy-api:8317
```

同时确认 CPA 配置包含 `remote-management.allow-remote: true`。

### VPN 显示连接但没有网络

```bash
docker compose logs --since=10m xui
ss -lntup | grep ':8443'
```

云安全组必须分别存在 `8443/TCP` 和 `8443/UDP`。Hysteria2 链接必须包含非空 auth；Reality 链接应包含 `security=reality`、`pbk`、`sid`、`sni` 和 `flow=xtls-rprx-vision`。

## 16. 私密数据

以下内容均按密码处理，不上传到公开仓库：

```text
.env
config/rurublog/application.yml
data/rurublog/
data/x-ui/x-ui.db
data/nginx-ui/database.db
data/letsencrypt/
data/bootstrap-output/access.json
```

## 17. 博客配置与资源

博客在 `https://xiaoruru.beer/admin/login` 登录。以 root 打开 `config/rurublog/application.yml`，使用其中的 `blog.admin.secret`；它与 3x-ui/Nginx UI 的管理员凭据独立。H2 密码是同一文件中的 `spring.datasource.password`，数据库创建后不要直接修改。

博客运行数据统一位于 `data/rurublog/`，私密 YAML 只读挂入容器，目录由 UID/GID 10001 管理。初始化不会覆盖已有凭据、数据，也不会导入开发目录 `xiaoruru-blog/data/`。需要开发数据时另行进行一致性迁移。

默认最大堆 320MiB、Metaspace 160MiB、容器上限 768MiB、CPU 上限 1 核、Tomcat 最大 32 线程、数据库最大 4 个连接；AI 默认关闭。本地 macOS 同参数启动、后台登录和 4 并发 100 次首页请求测试通过，Java RSS 约 417MiB；不是 Linux 服务器实测或生产容量承诺。部署后使用 `docker stats --no-stream`、`free -h` 检查所有服务的总内存。

配置细节、现有服务器接入步骤、博客更新方式见 [博客部署说明](./博客部署说明.md)。
