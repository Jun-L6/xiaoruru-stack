# xiaoruru Docker 完整部署说明

本文只描述在全新 Ubuntu 24.04 服务器上部署当前项目。部署过程会自动安装并配置 3x-ui、Nginx、Certbot、GOST、两个代理入站和一个主客户端。

## 1. 最终架构

```text
Internet
├── 80/TCP   -> Nginx（ACME 验证、HTTPS 跳转）
├── 443/TCP  -> Nginx（3x-ui 面板、订阅）
├── 8443/TCP -> 3x-ui/Xray（VLESS + RAW + Reality + Vision）
├── 8443/UDP -> 3x-ui/Xray（Hysteria2 + TLS）
└── 9443/TCP -> GOST（带账号密码的 HTTPS 正向代理）

Docker 内部网络
├── xui:2053   3x-ui 面板 HTTP
├── xui:2096   订阅服务 HTTP
├── nginx       公网 HTTPS 入口
├── gost        HTTPS 正向代理
└── panel-init  首次初始化任务，执行完即退出
```

2053 和 2096 不发布到宿主机，也不需要在云安全组中开放。

## 2. 持久化目录

项目固定部署到：

```text
/srv/xiaoruru-stack
```

所有业务数据集中在项目目录：

```text
data/x-ui/              3x-ui 数据库与配置
data/letsencrypt/       Let's Encrypt 证书和账户状态
data/certbot-webroot/   ACME HTTP-01 验证目录
data/logs/              Nginx 与 3x-ui 日志
data/nginx-runtime/     Nginx 模板生成结果
data/bootstrap-output/  私密订阅地址与节点链接
```

`.env` 保存管理员和 GOST 凭据，`data/bootstrap-output/access.json` 保存订阅地址与节点链接。这些内容都按私密凭据处理。

## 3. 服务器要求

- Ubuntu 24.04 LTS x86_64；
- root SSH 权限；
- 域名 `xiaoruru.beer` 和 `www.xiaoruru.beer` 的 A 记录指向服务器公网 IP；
- 云安全组允许：

| 端口 | 协议 | 用途 |
| --- | --- | --- |
| 22 | TCP | SSH |
| 80 | TCP | Let's Encrypt 和 HTTP 跳转 |
| 443 | TCP | 面板与订阅 HTTPS |
| 8443 | TCP | VLESS Reality |
| 8443 | UDP | Hysteria2 |
| 9443 | TCP | GOST HTTPS 正向代理 |

## 4. 安装 Docker

在服务器执行：

```bash
apt-get update
apt-get install -y ca-certificates curl gnupg
install -m 0755 -d /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg \
  -o /etc/apt/keyrings/docker.asc
chmod a+r /etc/apt/keyrings/docker.asc

. /etc/os-release
cat >/etc/apt/sources.list.d/docker.sources <<EOF
Types: deb
URIs: https://download.docker.com/linux/ubuntu
Suites: ${UBUNTU_CODENAME:-$VERSION_CODENAME}
Components: stable
Architectures: $(dpkg --print-architecture)
Signed-By: /etc/apt/keyrings/docker.asc
EOF

apt-get update
apt-get install -y docker-ce docker-ce-cli containerd.io \
  docker-buildx-plugin docker-compose-plugin sqlite3
systemctl enable --now docker
docker version
docker compose version
```

## 5. 上传部署目录

从本地电脑执行：

```bash
scp -i '/Users/jun/Downloads/初始化搭建.pem' -r \
  '/Users/jun/Documents/Codex/2026-08-12/new-chat/docker-xiaoruru' \
  root@198.11.173.173:/srv/xiaoruru-stack
```

上传后在服务器检查：

```bash
cd /srv/xiaoruru-stack
chmod 600 .env
chmod +x scripts/*.sh
./scripts/validate.sh
```

## 6. `.env` 配置

正式部署使用项目自带的 `.env`。关键配置如下：

```dotenv
DOMAIN=www.xiaoruru.beer
ROOT_DOMAIN=xiaoruru.beer

XUI_IMAGE=ghcr.io/mhsanaei/3x-ui:v3.7.0
NGINX_IMAGE=nginx:1.30.4-alpine
GOST_IMAGE=gogost/gost:3.2.6
CERTBOT_IMAGE=certbot/certbot:v5.7.0
PANEL_INIT_IMAGE=python:3.13.7-alpine3.22

HTTP_PORT=80
HTTPS_PORT=443
XRAY_PORT=8443
GOST_PORT=9443
XUI_PANEL_PORT=2053
XUI_SUB_PORT=2096

PRIMARY_CLIENT_EMAIL=jun-main
REALITY_MIN_CLIENT_VER=1.0.0
REALITY_TARGET_CANDIDATES=
```

`REALITY_TARGET_CANDIDATES` 留空时，初始化任务调用 3x-ui 的扫描接口，从内置公共候选站点中选择实际可用且满足 TLS 1.3、HTTP/2、X25519 和证书校验要求的目标。

管理员与 GOST 密码已保存在 `.env`，不要把该文件发到公开仓库或聊天群。

## 7. 一键部署

在服务器执行：

```bash
cd /srv/xiaoruru-stack
./scripts/bootstrap.sh
```

脚本会依次完成：

1. 创建统一数据目录；
2. 拉取固定版本容器镜像；
3. 启动 HTTP-only Nginx；
4. 初始化 3x-ui 数据库并写入管理员账号密码；
5. 为根域名和 `www` 域名签发 Let's Encrypt 证书；
6. 配置面板、可信代理和三种订阅服务；
7. 扫描 Reality 目标并生成 Reality X25519 密钥和 Short ID；
8. 创建 `VLESS-Reality-8443` 入站；
9. 创建 `Hysteria2-8443` 入站并绑定证书；
10. 创建 `jun-main` 客户端并绑定两个入站；
11. 为客户端生成 UUID、Hysteria auth、Sub ID 和分享链接；
12. 启动 Nginx、3x-ui/Xray 与 GOST。

初始化是幂等的：重复执行会复用同名入站和客户端，不会创建重复节点。

## 8. 自动创建的入站

### VLESS Reality

```text
备注：VLESS-Reality-8443
协议：VLESS
端口：8443/TCP
传输：RAW/TCP
安全：Reality
客户端 Flow：xtls-rprx-vision
分享地址：www.xiaoruru.beer
最低客户端版本：1.0.0
```

Reality 私钥只保存在 3x-ui 数据库中；客户端分享链接只包含公钥。

### Hysteria2

```text
备注：Hysteria2-8443
协议：hysteria version 2
端口：8443/UDP
SNI：www.xiaoruru.beer
ALPN：h3
UDP Idle Timeout：60 秒
```

容器内证书路径：

```text
/etc/letsencrypt/live/www.xiaoruru.beer/fullchain.pem
/etc/letsencrypt/live/www.xiaoruru.beer/privkey.pem
```

VLESS 使用 TCP，Hysteria2 使用 UDP，因此可以使用相同的数字端口 8443。

## 9. 访问面板和订阅

面板地址：

```text
https://www.xiaoruru.beer/
```

管理员账号密码：

```bash
cd /srv/xiaoruru-stack
grep -E '^XUI_ADMIN_(USERNAME|PASSWORD)=' .env
```

所有客户端访问资料保存在：

```bash
cd /srv/xiaoruru-stack
chmod 600 data/bootstrap-output/access.json
cat data/bootstrap-output/access.json
```

文件中包含：

- `subscriptions.base`：Shadowrocket、v2rayN 等可使用的基础订阅；
- `subscriptions.json`：Xray JSON 订阅；
- `subscriptions.clash`：Clash Mi/Mihomo 订阅；
- `links`：VLESS Reality 和 Hysteria2 单节点分享链接。

订阅地址和分享链接等同于登录凭据，不要公开。

## 10. GOST HTTPS 正向代理

代理参数：

```text
类型：HTTPS Proxy
服务器：www.xiaoruru.beer
端口：9443
用户名：.env 中的 GOST_USERNAME
密码：.env 中的 GOST_PASSWORD
```

从服务器外部测试：

```bash
curl \
  --proxy https://www.xiaoruru.beer:9443 \
  --proxy-user '用户名:密码' \
  https://api.ipify.org
```

返回值应为服务器公网出口 IP。

## 11. 部署验收

服务器侧执行：

```bash
cd /srv/xiaoruru-stack
docker compose ps
docker stats --no-stream
ss -lntup | grep -E ':(80|443|8443|9443)\b'
curl -fsS http://127.0.0.1/healthz
curl -I https://www.xiaoruru.beer/
```

确认数据库对象：

```bash
sqlite3 data/x-ui/x-ui.db \
  "SELECT id, remark, protocol, port, enable FROM inbounds ORDER BY id;"

sqlite3 data/x-ui/x-ui.db \
  "SELECT email, length(uuid), length(auth), flow, enable FROM clients;"
```

预期看到两个入站；主客户端的 UUID 和 Hysteria auth 长度均大于 0，Flow 为 `xtls-rprx-vision`。

从手机流量或另一条外部网络完成：

1. 打开面板，确认证书正常；
2. 导入基础订阅并测试 VLESS Reality；
3. 导入 Clash/Mihomo 订阅并测试；
4. 切换 Hysteria2 测试 UDP 线路；
5. 测试 GOST 正向代理。

## 12. 证书自动续期

手动检查：

```bash
cd /srv/xiaoruru-stack
./scripts/renew-certificate.sh
```

启用 systemd timer：

```bash
cd /srv/xiaoruru-stack
install -m 0644 config/systemd/xiaoruru-cert-renew.service /etc/systemd/system/
install -m 0644 config/systemd/xiaoruru-cert-renew.timer /etc/systemd/system/
systemctl daemon-reload
systemctl enable --now xiaoruru-cert-renew.timer
systemctl list-timers xiaoruru-cert-renew.timer --all
```

证书发生更新时，续期脚本会重启 Nginx、GOST 和 3x-ui，使所有 TLS 服务载入新证书。

## 13. 日常管理

```bash
cd /srv/xiaoruru-stack
docker compose ps
docker compose logs -f --tail=100 xui
docker compose logs -f --tail=100 nginx
docker compose logs -f --tail=100 gost
```

重启服务：

```bash
docker compose restart
```

重新执行初始化检查：

```bash
docker compose --profile ops run --rm panel-init
```

该命令只补齐缺失配置，不会覆盖已经存在的同名入站或主客户端。

## 14. 常见问题

### Certbot 返回 403

确认两个域名均解析到当前服务器，并且 80/TCP 可以从公网访问：

```bash
dig +short A xiaoruru.beer
dig +short A www.xiaoruru.beer
curl -i http://www.xiaoruru.beer/.well-known/acme-challenge/test
docker compose logs --tail=200 nginx
```

### VPN 显示连接但没有网络

依次检查：

```bash
docker compose logs --since=10m xui
sqlite3 data/x-ui/x-ui.db \
  "SELECT email, length(auth), flow, enable FROM clients;"
ss -lntup | grep ':8443'
```

同时确认云安全组分别存在 `8443/TCP` 和 `8443/UDP` 两条规则。Hysteria2 链接必须包含非空 auth；Reality 链接应包含 `security=reality`、`pbk`、`sid`、`sni` 和 `flow=xtls-rprx-vision`。

### Clash Mi 无法加载节点

使用 `access.json` 中的 `subscriptions.clash`，不要把基础订阅地址当成 Clash YAML 地址。客户端内核需要支持 VLESS Reality 和 Hysteria2。

## 15. 安全注意事项

- `.env`、`data/x-ui/x-ui.db`、TLS 私钥、`access.json` 都是敏感文件；
- 不开放 2053 和 2096；
- 不把订阅地址放入网页源码或公开仓库；
- 每台额外设备建议在 3x-ui 中创建独立客户端；
- Reality 私钥不能发送给客户端；
- 9443 必须保留强随机用户名和密码。
