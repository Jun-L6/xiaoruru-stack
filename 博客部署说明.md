# 将小茹茹博客部署到现有服务器

适用于 `/srv/xiaoruru-stack` 已部署 Nginx UI、3x-ui、GOST 的服务器。以下命令由你执行；本次项目集成没有连接或修改服务器。

## 1. 最终配置

- 站点：`https://xiaoruru.beer/`。
- 后台：`https://xiaoruru.beer/admin/login`，使用独立管理密钥。
- 容器服务：`rurublog`，Nginx 上游 `http://rurublog:8080`。
- 共享网络：`xiaoruru-gateway`，不新增公网端口。
- 数据：`data/rurublog/`，包括数据库、附件、备份、日志。
- 私密配置：`config/rurublog/application.yml`，只读挂入容器。
- 证书：与现有五个域名共用 `xiaoruru-services`，仍由 Certbot 续期。

这里只使用主域名 `xiaoruru.beer`。`www.xiaoruru.beer:9443` 仍用于 GOST，未增加 www 网站跳转。Nginx UI 中的博客站点为 `50-rurublog.conf`。

## 2. 本地构建 JAR

本机需要 JDK 21 和 Maven，不要求本机启动 Docker。

```bash
cd /Users/jun/IdeaProjects/docker-xiaoruru
mvn -f xiaoruru-blog/pom.xml -B -ntp verify \
  '-DargLine=-Dspring.config.location=classpath:/application.yml'
ls -lh xiaoruru-blog/target/rurublog.jar
```

构建包含博客自动化测试。Java 编译在本机执行；服务器使用 `prebuilt` 阶段将 JAR 放入 Java 21 运行镜像，不运行 Maven。JAR 可以从 Mac 上传到 Linux；服务器构建时使用服务器架构对应的基础镜像。

## 3. 上传部署文件

在本机执行。源目录末尾的 `/` 表示同步内容，不额外套一层目录。没有 `--delete`，不删除服务器数据；排除私密配置和本地数据，仅上传 target 中的 JAR。

```bash
cd /Users/jun/IdeaProjects/docker-xiaoruru
rsync -avz \
  --exclude '.git/' --exclude '.idea/' --exclude '.DS_Store' \
  --exclude '.env' --exclude '*.tar' --exclude '*.tar.gz' \
  --exclude '/data/' \
  --exclude '/config/rurublog/application.yml' \
  --exclude '/config/rurublog/application.yaml' \
  --exclude '/xiaoruru-blog/data/' \
  --exclude '/xiaoruru-blog/config/application.yml' \
  --exclude '/xiaoruru-blog/config/application.yaml' \
  --include '/xiaoruru-blog/target/' \
  --include '/xiaoruru-blog/target/rurublog.jar' \
  --exclude '/xiaoruru-blog/target/*' \
  -e 'ssh -i /Users/jun/Downloads/初始化搭建.pem' \
  /Users/jun/IdeaProjects/docker-xiaoruru/ \
  root@198.11.173.173:/srv/xiaoruru-stack/
```

## 4. 确认解析和配置

DNS 的 `@` A 记录应指向 `198.11.173.173`，80/TCP、443/TCP 保持开放。如果有 AAAA 记录，也必须指向能正常提供站点的服务器，错误的 IPv6 记录会影响证书验证。

在服务器执行：

```bash
cd /srv/xiaoruru-stack
apt-get update
apt-get install -y python3 openssl
chmod +x scripts/*.sh
dig +short A xiaoruru.beer
dig +short AAAA xiaoruru.beer
```

保留服务器原来的 `.env` 和密码，不要用本机整份 `.env` 覆盖。新增博客变量均有默认值，旧 `.env` 可以直接使用；想显式配置时，用 `nano .env` 加入以下内容，已有同名项直接修改，不要重复添加：

```dotenv
BLOG_DOMAIN=xiaoruru.beer
BLOG_IMAGE=xiaoruru/rurublog:local
BLOG_BUILD_TARGET=prebuilt
BLOG_UID=10001
BLOG_GID=10001
BLOG_MEMORY_LIMIT=768m
BLOG_CPUS=1.0
BLOG_JAVA_OPTIONS='-Xms64m -Xmx320m -XX:MaxMetaspaceSize=160m -XX:ReservedCodeCacheSize=64m -XX:MaxDirectMemorySize=64m -Xss512k -XX:+UseSerialGC -XX:ActiveProcessorCount=2 -XX:+ExitOnOutOfMemoryError'
```

保留 `CERT_NAME=xiaoruru-services`。博客公开 URL 和 HTTPS 安全 Cookie 由根 Compose 自动设置。

## 5. 构建运行镜像并接入博客

在服务器以 root 执行：

```bash
cd /srv/xiaoruru-stack
./scripts/validate.sh
docker build --target prebuilt -t xiaoruru/rurublog:local ./xiaoruru-blog
./scripts/deploy-blog.sh
```

若修改了 `BLOG_IMAGE`，镜像标签也要保持一致。需要从源码在服务器构建时，设置 `BLOG_BUILD_TARGET=runtime` 后使用 `./scripts/deploy-blog.sh --build`；容器运行内存限制不限制 Maven 构建内存，小内存服务器推荐 prebuilt。

脚本会：

1. 检查共享网络、Compose 和运行中的 Nginx。
2. 创建博客目录、设置 UID/GID，仅在配置不存在时生成随机管理员密钥和数据库密码。
3. 启动博客，等待容器健康检查通过。
4. 检查共享证书；缺少主域名时在原证书名称下扩展 SAN，保留原有域名。
5. 新增博客 HTTPS 站点并热重载 Nginx；已在 Nginx UI 修改的博客配置不覆盖。

不会重建 VPN 入站或客户端。扩展共享证书成功后，会重启正在运行的 GOST 和 3x-ui 来加载新证书，代理连接会有短暂中断。证书已包含主域名且未过期时，跳过签发和这两项重启。已有服务器不要为了新增博客执行完整 `bootstrap.sh`。

共享证书是一张包含多个明确域名的 SAN 证书，不是通配符证书。原五个域名加主域名共六个，路径不变：

```text
/etc/letsencrypt/live/xiaoruru-services/fullchain.pem
/etc/letsencrypt/live/xiaoruru-services/privkey.pem
```

Certbot 通过现有 Nginx 的 `/.well-known/acme-challenge/` 完成 HTTP-01 验证。不要在 Nginx UI 中再为这张共享证书启用另一套自动签发/续期。所有证书域名需要维持可用的验证路由；验证失败时脚本停止，不启用尚无有效证书的博客 HTTPS 站点。

## 6. 登录与验收

```bash
cd /srv/xiaoruru-stack
docker compose ps rurublog
docker compose logs --tail=100 rurublog
docker compose exec -T rurublog /app/healthcheck.sh
docker compose exec -T nginx-ui nginx -t
curl -I http://xiaoruru.beer/
curl -I https://xiaoruru.beer/
curl -I https://xiaoruru.beer/admin/login
docker stats --no-stream
docker ps --format 'table {{.Names}}\t{{.Ports}}'
openssl x509 -in data/letsencrypt/live/xiaoruru-services/fullchain.pem -noout -text
```

预期：容器 healthy，HTTP 跳 HTTPS，HTTPS 首页/登录页返回 200，证书 SAN 包含 `xiaoruru.beer`。博客可以显示 `8080/tcp`，但不应有 `0.0.0.0:8080->8080` 映射。公网 `/actuator/health` 返回 404 是预期行为，容器内部仍可检查。

以 root 打开私密配置查看 `blog.admin.secret`：

```bash
nano /srv/xiaoruru-stack/config/rurublog/application.yml
```

在 `https://xiaoruru.beer/admin/login` 填写该密钥，它独立于 Nginx UI、3x-ui 和 GOST。不要公开配置文件。修改管理密钥后运行 `docker compose restart rurublog`；不要直接修改已经创建数据库的 `spring.datasource.password`。

## 7. 内存与资源

集成配置以根 Compose 为准，不使用博客独立 Compose 中的资源参数：

| 项目 | 集成默认值 |
| --- | --- |
| JVM 初始/最大堆 | 64MiB / 320MiB |
| Metaspace 上限 | 160MiB |
| JIT 代码缓存上限 | 64MiB |
| Direct Memory 上限 | 64MiB |
| 容器内存上限 | 768MiB |
| CPU 上限 | 1 核 |
| Tomcat 最大线程 | 32 |
| 数据库连接池 | 最大 4，最小空闲 1 |
| AI | 默认关闭 |

本地 macOS 使用相同 JVM 参数，启动、后台登录、安全 Cookie、4 并发 100 次首页请求均通过，测试结束时 Java RSS 约 417MiB。这不是 Linux 容器或生产压力测试结果。768MiB 是容器上限，不是固定占用；文章数据量、上传、并发和 AI 功能会改变实际占用。

服务器同时运行 VPN、Nginx UI、CPAMP 时，需要观察整机余量和所有容器的 `docker stats`，不能只看博客：

```bash
docker inspect xiaoruru-rurublog --format '{{.State.OOMKilled}} {{.RestartCount}}'
free -h
```

调整 `.env` 中的资源参数后，运行 `docker compose up -d --no-build --wait rurublog`。仅 `restart` 不会更新环境变量或资源限制。

## 8. 数据与后续更新

不会自动复制或修改开发目录 `xiaoruru-blog/data/` 和它的私密配置，服务器首次启动使用独立的新博客数据库。带入开发数据需要另行进行一致性迁移，不能覆盖正在写入的 H2 文件，也不能丢失原数据库密码。

博客运行数据全部位于 `data/rurublog/`，私密配置位于 `config/rurublog/application.yml`。迁移整套服务时，这两部分和项目原有 `data/`、`.env` 都要保留。应用备份也在该数据目录内，但不替代独立保存私密配置。

更新代码后重复“本机构建 JAR → rsync 上传 → 服务器 prebuilt 构建 → deploy-blog.sh”。不要在服务器 `xiaoruru-blog/` 目录内再执行独立 Compose。

证书继续使用 `scripts/renew-certificate.sh` 和 `xiaoruru-cert-renew.timer`；续期后 Nginx 热重载、GOST/3x-ui 重启，博客无需重启，因为 TLS 在 Nginx 终止。
