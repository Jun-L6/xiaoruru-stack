# 小茹茹博客私密配置模板：复制为同目录 application.yml 后填写。
# 不要提交真实密钥。应用从项目根目录启动时自动读取，Docker 只读挂载本目录。
blog:
  admin:
    # 至少 12 个字符，UTF-8 不超过 72 字节。修改后重启生效。
    secret: ''
    # 如果配置了 secret-hash（含环境变量），则优先使用哈希而非上面的密钥。
