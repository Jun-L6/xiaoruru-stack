# Allowlist: never send secrets, user data, IDE state, or QA artifacts to Docker.
**
!Dockerfile
!pom.xml
!src/
!src/**
!deploy/
!deploy/docker/
!deploy/docker/entrypoint.sh
!deploy/docker/healthcheck.sh
!target/
!target/rurublog.jar
